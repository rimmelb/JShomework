<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8">
  <title>Edit Goal</title>
  <link rel="stylesheet" href="/css/styles_goals.css">
</head>
<body>
  <header>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/workouts">Training</a></li>
        <li><a href="/goals" class="active">Goals</a></li>
        <li><a href="/records">Records</a></li>
      </ul>
    </nav>
  </header>
  <main>
    <h2>Edit Goal</h2>
    <% if (errors.length > 0) { %>
      <ul class="errors">
        <% errors.forEach(function(error){ %>
          <li><%= error.msg %></li>
        <% }); %>
      </ul>
    <% } %>
    <form action="/goals/<%= goal._id %>/edit" method="POST">
      <label for="date">Date:</label>
      <input type="date" name="date" id="date" value="<%= goal.date ? goal.date.toISOString().split('T')[0] : '' %>" required>
      <br>
      <label for="exercise">Exercise:</label>
      <select name="exercise" id="exercise" required>
        <option value="">-- Select Exercise --</option>
        <option value="Weightlifting" <%= goal.exercise === "Weightlifting" ? "selected" : "" %>>Weightlifting</option>
        <option value="Running" <%= goal.exercise === "Running" ? "selected" : "" %>>Running</option>
        <option value="Cycling" <%= goal.exercise === "Cycling" ? "selected" : "" %>>Cycling</option>
        <option value="Swimming" <%= goal.exercise === "Swimming" ? "selected" : "" %>>Swimming</option>
      </select>
      <label for="exerciseType">Exercise Type:</label>
      <select name="exerciseType" id="exerciseType" data-current="<%= goal.exerciseType %>" required>
        <option value="">-- Select Type --</option>
      </select>
      <br>
      <label for="goal">Goal:</label>
      <input type="text" name="goal" id="goal" value="<%= goal.goal %>" required>
      <br>
      <button type="submit" class="modify">Save</button>
    </form>
    <a href="/goals">Back to Goals</a>
    <script src="/js/exerciseTypes.js"></script>
  </main>
</body>
</html>