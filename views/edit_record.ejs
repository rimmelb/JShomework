<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8">
  <title>Edit Record</title>
  <link rel="stylesheet" href="/css/styles_records.css">
</head>
<body>
  <header>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/workouts">Training</a></li>
        <li><a href="/goals">Goals</a></li>
        <li><a href="/records" class="active">Records</a></li>
      </ul>
    </nav>
  </header>
  <main>
    <h2>Edit Record</h2>
    <% if (errors.length > 0) { %>
      <ul class="errors">
        <% errors.forEach(function(error) { %>
          <li><%= error.msg %></li>
        <% }); %>
      </ul>
    <% } %>
    <form action="/records/<%= record._id %>/edit" method="POST">
      <label for="date">Date:</label>
      <input type="date" id="date" name="date" value="<%= record.date ? record.date.toISOString().split('T')[0] : '' %>" required>
      <br>
      <label for="exercise">Exercise:</label>
      <select id="exercise" name="exercise" required>
        <option value="">-- Select an exercise --</option>
        <option value="Weightlifting" <%= record.exercise === "Weightlifting" ? "selected" : "" %>>Weightlifting</option>
        <option value="Running" <%= record.exercise === "Running" ? "selected" : "" %>>Running</option>
        <option value="Cycling" <%= record.exercise === "Cycling" ? "selected" : "" %>>Cycling</option>
        <option value="Swimming" <%= record.exercise === "Swimming" ? "selected" : "" %>>Swimming</option>
      </select>
      <br>
      <label for="exerciseType">Exercise Type:</label>
      <select id="exerciseType" name="exerciseType" required>
        <option value="">-- Select a type --</option>
        <% types.forEach(function(type) { %>
          <option value="<%= type %>" <%= record.exerciseType === type ? "selected" : "" %>><%= type %></option>
        <% }); %>
      </select>
      <br>
      <label for="record">Record:</label>
      <input type="text" id="record" name="record" value="<%= record.record %>" required>
      <br>
      <button type="submit">Save</button>
    </form>
    <a href="/records">Back to Records Page</a>
    <script src="/js/exerciseTypes.js"></script>
  </main>
</body>
</html>
